# CRJ Day and Night

A minimal React and TypeScript site with static HTML, CSS, and JavaScript output.

## Development

```sh
nix develop
npm install
npm run dev
```

Create a production build with:

```sh
npm run build
```

The deployable site is written to `dist/`.
